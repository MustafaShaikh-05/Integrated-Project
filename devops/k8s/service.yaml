# NodePort Service — exposes the app cluster-wide on port 30080 (NodePort).
apiVersion: v1
kind: Service
metadata:
  name: insurevision-service
spec:
  type: NodePort
  selector:
    app: insurevision
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
