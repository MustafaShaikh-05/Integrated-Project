# Local multi-container orchestration (single app service) for InsureVision AI.
version: '3.8'
services:
  app:
    build:
      context: ..
      dockerfile: devops/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
